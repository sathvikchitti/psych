in backend:
pip install -r requirements.txt --only-binary=:all:
pip install transformers -U
python app.py

in code(for frontend):
npx serve .


backend when deployed
https://sense123-psychsense.hf.space.
